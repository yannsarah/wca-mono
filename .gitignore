# === Général ===
node_modules/
.DS_Store
._*
*.log
npm-debug.log*

# Anciennes versions (conservées hors Git comme archive)
_ARCHIVE/

# === gestion/ : données vivantes (secret de session + données réelles) ===
# Sauvegardées séparément. Structure vide versionnée : gestion/data.example.json
gestion/data.json
**/data.json

# Médiathèque : fichiers images téléversés (vivent sur le serveur, jamais dans Git)
gestion/public/medias/

# Documents asso (PV, convocations… ) : fichiers sur le serveur, jamais dans Git
gestion/docs/

# === site/ : secrets & sources à ne jamais publier ===
.deploy/
**/.deploy/
*.pem
deploy.config*
o2switch_deploy*

# Archives & export WordPress (source, pas à versionner)
*.zip
*.xml
!site/sitemap.xml

# Temporaire généré par migrate-images.sh
site/img/_urls.txt
